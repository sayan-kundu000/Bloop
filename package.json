{
  "name": "bloop-workspace",
  "version": "1.0.0",
  "private": true,
  "description": "Bloop — AI Text-to-Speech & Quantum Intelligence Platform",
  "scripts": {
    "dev": "npm --prefix frontend run dev",
    "start": "npm --prefix frontend run dev",
    "build": "npm --prefix frontend run build",
    "test": "npm --prefix frontend run test",
    "lint": "npm --prefix frontend run lint",
    "server": "uvicorn backend.app.main:app --reload --host 127.0.0.1 --port 8000",
    "server:win": "py -3.12 -m uvicorn backend.app.main:app --reload --host 127.0.0.1 --port 8000",
    "test:backend": "py -3.12 -m pytest backend/tests/ -v"
  },
  "keywords": [
    "ai",
    "text-to-speech",
    "tts",
    "quantum-intelligence",
    "fastapi",
    "react",
    "vite"
  ],
  "author": "",
  "license": "MIT"
}
